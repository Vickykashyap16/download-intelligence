# ⚠️ Deprecated — superseded by `Database/`

This folder is no longer used. It's kept in place only because files here can't be deleted from this workspace.

The persistent storage design now lives in `Database/` (split into `Database/Metadata/`, `Database/FileIndex/`, `Database/History/`). See `Database/README.md` for the current design.

Ignore this folder going forward.
