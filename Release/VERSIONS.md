# Version Ledger — Downloads Intelligence

Tracks the overall pipeline version and each module's independent version. This is the source of truth for both — update it every time a module is released or re-released.

## Current

**Pipeline Version: 0.8.0**

| Module | Version | Status |
|---|---|---|
| 01 — Watch & Ingest | 1.0.1 | Released |
| 02 — Classification | 1.0.1 | Released |
| 03 — Metadata Extraction | 1.0.0 | Released |
| 04 — Duplicate & Version Detection | 1.0.1 | Released |
| 05 — Naming & Destination | 1.0.0 | Released |
| 06 — Confidence & Review | 1.0.0 | Released |
| 07 — Preview, Approval & Execution | 1.0.0 | Released |
| 08 — Logging & Reporting | 1.0.0 | Released |

## Validated Baselines

Distinct from "Released" (above — independently verified against a module's own written contract, per `Governance/ENGINEERING_STANDARD.md`). A **Validated Baseline** additionally records that a specific Pipeline Version was carried through the Real-World Validation Framework (`REAL_WORLD_VALIDATION_PLAN.md`) against genuine, uncurated content, with the results independently reviewed. This section exists so future validation runs and future releases have an unambiguous reference point to compare against (`BENCHMARK_SPECIFICATION.md`'s own cross-release comparison design), and so "validated" always states precisely what evidence backs it, rather than being read as a stronger, unqualified claim.

| Baseline Tag | Pipeline Version | Frozen | Authorized by |
|---|---|---|---|
| `v0.8.0-validated-baseline` | 0.8.0 | 2026-07-20 | `PROJECT_REVIEW_BOARD_REPORT.md` §8 ("Should v0.8.0 now be frozen as the validated baseline? Yes, with an explicit scope caveat.") |

**`v0.8.0-validated-baseline` — scope of what "validated" means here, stated precisely rather than left implicit:**
- **Evidence basis:** 3 real-world validation runs (`VALIDATION_LEDGER.md` Runs 001–003), 2 of which executed against real, uncurated content (Run 002: 9 files, one content shape; Run 003: 36 files processed / 14 executed, a systematic top-level sample of the real Downloads folder) — all on a single calendar day, 2026-07-20.
- **What held, with direct evidence:** zero data-loss or unauthorized-action findings across every real execution attempted; correct discrimination between what to file and what to withhold under genuinely mixed, partly-wrong upstream classification (`PATTERN_TRACKER.md` PT-004, Confirmed Pattern); genuine malformed-input handling confirmed against a real, organically-occurring corrupted file (PT-010).
- **What was NOT tested and remains open, stated explicitly rather than implied "fine":** undo/reversibility against real executed content (zero attempts in any run); the project's own pre-declared evidence-volume bar (`REAL_WORLD_VALIDATION_PLAN.md` §9, criterion A4 — ≥3 sessions across ≥2 calendar weeks, ≥150 files; actual: 2 sessions, 1 calendar day, ~45 files); unattended/Scheduled execution mode (every run to date substituted live judgment for the missing autonomous provider, `TECHNICAL_DEBT_REGISTER.md` TD-01).
- **Known, confirmed quality gaps at this baseline (not safety gaps):** `PT-002` (Screenshot misclassification of real non-screenshot images) and `PT-003` (false-positive version-chain grouping) — both Confirmed Pattern, both Medium severity, both never resulted in an incorrect automated filing in any run.
- **Release readiness recommendation attached to this baseline:** Ready for Limited Production (`PROJECT_REVIEW_BOARD_REPORT.md` §4) — attended/interactive operation, continued spot-checking of `auto`-tier output, not yet a basis for certifying unattended/Scheduled operation.
- Future validation runs and future Pipeline Version releases should compare their own results against this baseline's recorded findings (`PATTERN_TRACKER.md` as of 2026-07-20) to detect regression or genuine improvement, per `BENCHMARK_SPECIFICATION.md`'s comparison design.

## Versioning convention

- **Module Version** — standard semver (`MAJOR.MINOR.PATCH`), independent per module:
  - `PATCH` — bug fix within the module's current, frozen contract (e.g. the symlink fix would have been a patch release had this convention existed at the time).
  - `MINOR` — additive change that doesn't break the module's `MODULE_CONTRACT.md` (e.g. a new optional field, a new supported extension).
  - `MAJOR` — a change to the module's contract (its declared INPUT/OUTPUT/guarantees) that could require downstream modules to adapt.
- **Pipeline Version** — one number for the whole project's overall maturity. Bumped deliberately at meaningful milestones (e.g. "all 8 modules built and passing end-to-end" would justify `1.0.0`), not automatically derived from module versions.
- Modules evolve independently and at different paces. Illustrative example of where this goes (not the current state): `Pipeline 0.4.0` with `Module01 v1.0.0`, `Module02 v1.2.0`, `Module05 v0.9.0` — each module has iterated on its own schedule; the pipeline number reflects overall progress, not an average or maximum of the module numbers.
- **This file (`Release/VERSIONS.md`) is the single authoritative source of truth for every module's current version** (line 3, above) — it is updated on every release or re-release, including post-freeze patches. Each module's own `MODULE_STATUS.md` (in `Release/ModuleNN/`) is, by contrast, a **permanent, point-in-time release record**, generated once and never updated afterward — every existing instance (Modules 01–05) explicitly self-labels as "the permanent release record for Module NN," and every one of their `Pipeline Version` fields stays fixed at whatever the pipeline's version was on the date that module's record was created, never bumped as the pipeline version later advances. **Clarified 2026-07-11** (Module 01 post-freeze correction #1, see `Release/Module01/RELEASE_NOTES.md`): this line previously read "records its current version," which was imprecise and could be misread as implying MODULE_STATUS.md is kept live — it is not, and by design. If a module's version changes after its `MODULE_STATUS.md` was generated, this ledger (and the affected module's own dated `RELEASE_NOTES.md` addendum) is what reflects that; `MODULE_STATUS.md` correctly continues to describe the module's state as of its own generation date, exactly as `CHANGELOG.md` entries do for their own dates.

## History

- **2026-07-23** — **Module 04 patched `1.0.0` → `1.0.1` (PT-003 closed), later same day.** Pipeline Version unchanged (still 0.8.0). Post-freeze correction under `Governance/FROZEN_MODULE_CHANGE_POLICY.md`: `_check_version_chain()`'s candidacy check no longer accepts a same-category, above-similarity-threshold filename pair as a version-chain group on filename evidence alone — it now additionally requires either an explicit version token on at least one side, or an identical normalized filename corroborated by a size-proximity check (`PATTERN_TRACKER.md` PT-003, Confirmed Pattern, real-world evidence from Runs 002/003). Patch-level per this file's own convention: no change to `MODULE_CONTRACT.md`'s INPUT/OUTPUT/guarantees — the contract makes no precision/recall claim on `version_group_id`/`version_rank` this narrows. Full sequence: Design (`Build-out/04 Duplicate & Version Detection/Module 04 Post-Freeze Design Correction — PT-003.md`, 2 revisions) → independent review (2 rounds — Round 1 APPROVE WITH CHANGES on findings E1/S1; Round 2 APPROVE after both resolved and one new Low finding, a zero-byte edge case, caught and closed in the same pass) → Implementation (`src/pipeline/duplicate_detector.py` only — `src/storage/database.py` untouched) → Regression (729/729, including 9 new tests) → Re-validation against the same original Run 002/Run 003 real-world datasets (`PT003_VALIDATION_REPORT.md`, verdict PASS WITH NOTES — 4/4 real-world occurrences resolved, zero unintended changes elsewhere) → Close (`PATTERN_TRACKER.md`, `VALIDATION_LEDGER.md`). Full narrative: `PT003_POSTMORTEM.md`. **This is Module 04's fifth disclosed post-freeze correction** (following the pre-release UAT-1 near-duplicate category-scoping fix cited as "post-freeze correction #4" in the 2026-07-08 entry below, and the smaller idempotency/tie-break corrections made during its own Independent Implementation Audit cycle before first release) — see `Release/Module04/RELEASE_NOTES.md` for the numbered, itemized correction history. **Disclosed, not swept under this entry:** the 13-check Pipeline Contract Verification gate was **not** re-run as part of this closure. This is notable for a reason worth stating plainly rather than glossing over: `ENGINEERING_CHANGE_PLAYBOOK.md` §6 — written specifically in response to PT-002's own closure skipping this same gate — names the PCV gate as a required merge-criteria step for any Medium-or-higher-severity change, and PT-003 is Medium severity. This closure's own governance-and-release cycle did not include an explicit instruction to run it, and none was run; real-world re-validation plus the full regression suite were the evidence actually gathered. This is the same gap PT-002's own postmortem identified as "the single clearest process gap" in this project's engineering lifecycle, now recurring once on the very next Medium-severity change after the playbook meant to prevent it was written — recorded here honestly as a live process finding, not retroactively closed by this entry, and worth the project owner's attention independent of PT-003 itself. `Release/Module04/MODULE_STATUS.md` was deliberately **not** edited (point-in-time snapshot convention) — this ledger is the authoritative current-version source.
- **2026-07-23** — **Module 02 patched `1.0.0` → `1.0.1` (PT-002 closed).** Pipeline Version unchanged (still 0.8.0 — a module patch is not a pipeline milestone). Post-freeze correction under `Governance/FROZEN_MODULE_CHANGE_POLICY.md`: `classify_screenshot_or_image()`'s Screenshot-vs-Image split no longer treats "no camera EXIF present" as an independent, sufficient trigger for `Screenshot` (`PATTERN_TRACKER.md` PT-002, Confirmed Pattern, real-world evidence from Runs 002/003). Patch-level per this file's own convention: no change to `MODULE_CONTRACT.md`'s INPUT/OUTPUT/guarantees. Full sequence: Design (`Build-out/02 Classification/Module 02 Post-Freeze Design Correction — PT-002.md`, 2026-07-20) → Implementation (`src/pipeline/classification.py`, `Rules/Classification Rules.md`) → Regression (720/720, including 4 new tests) → Re-validation against the original Run 002/Run 003 real-world datasets (`PT002_VALIDATION_REPORT.md`, verdict PASS — 27/27 real-world occurrences resolved, zero unintended changes elsewhere) → Close (`PATTERN_TRACKER.md`, `TECHNICAL_DEBT_REGISTER.md` TD-16 closed). Full narrative and lifecycle evaluation: `PT002_POSTMORTEM.md`, `PT002_WORKFLOW_EVALUATION.md`. **Disclosed, not swept under this entry:** unlike Module 01's own 2026-07-11 patch (which re-ran the 13-check PCV gate as part of closing out), this closure did **not** separately re-run the PCV gate — real-world re-validation was judged sufficient evidence for this specific, narrow, deterministic-logic change, but this is a deliberate scope note, not an oversight, and `ENGINEERING_CHANGE_PLAYBOOK.md` now names the gate as a required step for future changes of this kind. `Release/Module02/RELEASE_NOTES.md` carries the full technical record (severity, root cause, fix, scope, verification) mirroring Module 01's own "Post-freeze correction" format exactly. Following that same precedent, `Release/Module02/MODULE_STATUS.md` was deliberately **not** edited (point-in-time snapshot convention) — this ledger is the authoritative current-version source.
- **2026-07-20** — **`v0.8.0-validated-baseline` recorded (see "Validated Baselines" above).** Not a code change and not a Pipeline Version bump (still 0.8.0) — a formal declaration that Pipeline Version 0.8.0 is the project's Validated Baseline, authorized by `PROJECT_REVIEW_BOARD_REPORT.md` following its independent review of `VALIDATION_LEDGER.md`, `PATTERN_TRACKER.md`, `VALIDATION_PROGRESS.md`, `VERSION_091_IMPLEMENTATION_PLAN.md`, `TECHNICAL_DEBT_REGISTER.md`, `PROJECT_RETROSPECTIVE.md`, and `Governance/FROZEN_MODULE_CHANGE_POLICY.md`. The Review Board's recommendation ("Ready for Limited Production," §4) and its explicit scope caveats are carried into the Validated Baselines entry above rather than repeated here. Immediately following this declaration, a read-only root-cause investigation for `PATTERN_TRACKER.md` PT-003 was authorized and completed (see `VALIDATION_LEDGER.md`'s "Post-Run 003 addendum"), and Design-phase work (not implementation) was authorized to begin for PT-002 only (`Build-out/02 Classification/Module 02 Post-Freeze Design Correction — PT-002.md`) — both per the Review Board Report's §7/§8 recommendations. No Module 01–08 source file was touched by any of this.
- **2026-07-20** — **Pipeline 0.8.0.** Module 08 (Logging & Reporting) released at **v1.0.0** — eighth and final module of the v1 pipeline, frozen and feature-complete. The only module in the pipeline that owns zero `FileRecord` fields, introduces no new `Database/` structure, and adds no new action-log value — the cleanest ownership boundary of any module released so far, and the only module with no disclosed gap between interactive and autonomous/unattended operation (no Provider, no judgment-dependent step of any kind). Following the Release Audit (RWP-D) certifying PASS, the full `Release/Module08/` package was generated (`MODULE_CONTRACT.md`, `MODULE_STATUS.md`, `TEST_RESULTS.md`, `PRODUCTION_CHECKLIST.md`, `KNOWN_LIMITATIONS.md`, `RELEASE_NOTES.md`, `RELEASE_SUMMARY.md`). Zero Critical, High, or Medium findings were found at any stage of this module's entire lifecycle (design, four review rounds, five Open Decisions each resolved via a dedicated Architecture Decision, WP-1 through WP-7, the WP-1–6 integration audit, the Independent Implementation Audit, Integration Testing, UAT, and the Release Audit) — a genuine first among the eight modules. Full regression suite 716/716 throughout (148 tests directly attributable to Module 08's own implementation). All eight modules of the v1 Downloads Intelligence pipeline are now individually built, tested, and released. **Pipeline Version 1.0.0 ("all 8 modules built and passing end-to-end") remains its own separate, deliberate milestone declaration per `Governance/ARCHITECTURE_DECISIONS.md` decision 14 — not automatically reached by this release.** Full detail: `Release/Module08/RELEASE_SUMMARY.md`.
- **2026-07-20** — Pipeline 0.7.0 (unchanged — a Release Audit is not itself a release). Module 08 (Logging & Reporting) — no version change (not yet released, still `—`); status updated from "Design Frozen — Not Implemented" to "Implementation Complete (WP-1–7); Release Audit PASS." Since design freeze (2026-07-14), Module 08 completed implementation (WP-1 through WP-7, all five Open Decisions resolved — `Governance/ARCHITECTURE_DECISIONS.md` decisions 25–31), a fresh Independent Implementation Audit (RWP-A, PASS WITH RECOMMENDATIONS — deferred maintenance only), Integration Testing (`Tests/Module 08 Integration Test Plan.md`, RWP-B, zero findings against the real Module 01→08 chain), User Acceptance Testing (`Tests/Module 08 UAT Plan.md`, RWP-C, PASS WITH RECOMMENDATIONS — two disclosed non-blocking observations, no defect), and an Independent Release Audit (`Release/Module08/RELEASE_AUDIT.md`, RWP-D): all 13 Pipeline Contract Verification checks pass (12 outright, 1 post-correction — three stale status documents, resolved in the same pass), all 31 Architecture Decisions independently confirmed compliant, a fresh release-certified performance measurement taken (2,000 records / 14,000 log lines, 0.5436s total, no regression). Full regression suite 716/716 throughout; zero Module 01–07 source file touched at any point. No `Release/Module08/` release-engineering package has been generated yet — Release (RWP-E) has not begun, pending separate explicit approval.
- **2026-07-14** — Pipeline 0.7.0 (unchanged — a design freeze is not a release). Module 08 (Logging & Reporting) — no version change (not yet released, still `—`); status updated from "Not started" to "Design Frozen — Not Implemented (frozen 2026-07-14)." Module 08's design (`Build-out/08 Logging & Reporting/Module 08 Design.md`) completed four independent design review rounds (`Module 08 Design Review.md`), resolving four Medium findings (M1–M4: a conditionally-defined idempotency guarantee, four broken cross-references to a non-existent §25, a blended performance cost model, and a missing design-time field taxonomy for `extracted_metadata` display), five Low and two Cosmetic findings (L1–L3, N1–N2, C1–C2), and a final two Low findings (N3–N4, both mis-cited Architecture Decision references caught only on a fresh, independent citation-by-citation re-verification against the real `Governance/ARCHITECTURE_DECISIONS.md`) across three corrective/cleanup/citation-correction passes. Round 4 converged on zero remaining Critical/High/Medium/Low findings and zero unresolved Cosmetic findings — the first round in this design's history to close clean. The project owner explicitly approved freezing the design, distinct from the review itself; see `Module 08 Design Review.md`'s "Freeze Record (2026-07-14)" section and `CHANGELOG.md`'s matching entry for full detail. No implementation code exists; Modules 01–07 remain untouched and permanently frozen. The five Open Decisions (OD-1 through OD-5) and two required documentation follow-ups (§25) are carried forward unresolved — freezing the architecture does not resolve them. `Governance/ARCHITECTURE_DECISIONS.md` required no new entry: no new architectural decision was introduced anywhere across all four review rounds (both Round 4 corrections were citation swaps only, correcting mis-references to existing decisions/guarantees, inventing nothing new). Implementation requires its own separate, explicit future approval (Implementation Planning, then WP-0) before it begins.
- **2026-07-13** — **Pipeline 0.7.0.** Module 07 (Preview, Approval & Execution) released at **v1.0.0** — seventh module in the pipeline, frozen and feature-complete. The first module in this pipeline to perform real, filesystem-mutating actions (moving, renaming, archiving real files) and the first capable of undoing its own work. This release completes the path the entry directly below left open: following that entry's blocking High finding (Integration Testing and UAT never performed), both stages were approved and executed. Integration Testing (`Tests/Module 07 Integration Test Plan.md`) ran a real harness against the full Module 01→07 chain — isolated storage, routing fake Module 02/03 providers — covering all three tiers, adversarial forged-decision resistance, a real execution-time collision re-check, both decision-23 override cases, forced-failure/partial-batch continuation, crash/restart reconciliation, CLI-level idempotency, and undo at both granularities: **71/71 checks passed, zero findings.** Along the way, before Integration Testing began, a genuine Medium-severity test-isolation defect was found in the existing regression suite itself (8 tests in `test_execution.py` silently writing synthetic data into the real project database on every run since the WP-7 persistence correction) — found, reported, approved, and resolved (the smallest fix: add the missing isolation call to exactly those 8 functions), with a scripted, function-by-function audit confirming no other test in the suite carries the same gap. UAT (`Tests/Module 07 UAT Plan.md`) then ran against a real external Downloads-like folder and a real external destination-library folder, real live-Claude-judged Module 02/03 content, and — per Module 06 UAT's own established precedent — the real project `Database`/`Runtime`: three real `main.execute()` invocations exercising the same dimension set against real human approval decisions, **zero findings**, plus the required 75-file performance measurement (40.116s for the complete Module 01→07 chain, versus Module 06's own 40.122s Module 01→06 baseline on the same dataset — no measurable regression). The Release Audit was re-run fresh after both stages completed and certified Module 07 release-ready: all 24 Architecture Decisions, all 10 Guarantees, all 7 Non-Guarantees, all 8 Invariants, and all 13 Pipeline Contract Verification checks pass, zero unresolved Critical/High/Medium findings (see `Release/Module07/RELEASE_AUDIT.md`). Full regression suite 568/568 passing throughout. No Module 01–06 contract was touched at any point. Module 08 (Logging & Reporting) is next, not yet started.
- **2026-07-13** — Pipeline 0.6.0 (unchanged — implementation completion is not a release, superseded by the entry above). Module 07 (Preview, Approval & Execution) — no version change (not yet released, still `—`); status updated from "Design frozen — not yet implemented" to "Implementation Complete (WP-1–13) — Release Audit BLOCKED." Module 07's full implementation (twelve work packages: foundational data structures through CLI wiring, plus documentation follow-ups) completed with one High finding found and resolved (WP-7's `ExecutionEngine` persistence gap) and three Medium findings found and resolved (WP-2's destination-override ambiguity, its corrective implementation, and an Implementation Plan documentation-consistency gap) — zero unresolved Critical/High/Medium findings remain from the implementation lifecycle itself (see `Release/Module07/IMPLEMENTATION_AUDIT.md`). A full `Release/Module07/` release-engineering package was generated and a formal Release Audit performed against all 24 Architecture Decisions, G1–G10, NG1–NG7, I1–I8, and all 13 Pipeline Contract Verification checks — the audit found one blocking High finding: **Integration Testing and UAT have never been performed for Module 07**, a hard requirement under this project's own stated lifecycle (`Governance/PROJECT_ROADMAP.md`'s non-negotiables), and no measured performance number exists for the real Module 01→07 chain (PCV check 12). Per explicit instruction, the audit stopped at this finding rather than certifying release-ready or proceeding further — see `Release/Module07/RELEASE_AUDIT.md` for the full record and recommended smallest correction. Full regression suite 568/568 passing. Module 08 not yet started, blocked on Module 07's own eventual release.
- **2026-07-12** — Pipeline 0.6.0 (unchanged — a design freeze is not a release). Module 07 (Preview, Approval & Execution) — no version change (not yet released, still `—`); status updated from "Not started" to "Design frozen (2026-07-12) — not yet implemented." Module 07's design (`Build-out/07 Preview, Approval & Execution/Module 07 Design.md`) completed three independent design review rounds, resolved four Medium findings (M1 imprecise determinism, M2 missing idempotency guard, M3 unresolved `Rules/Folder Rules.md` precedence ambiguity, M4 overstated crash-atomicity guarantee) across two correction cycles, and converged on zero remaining Critical/High/Medium findings. The project owner explicitly approved freezing the design, distinct from the review itself; the four remaining Low/Cosmetic findings (L1, L2, C1, C2) were each given an explicit recorded disposition (deferred / planned implementation clarification / accepted) rather than silently dropped — see `Module 07 Design Review.md`'s "Freeze Record (2026-07-12)" section and `CHANGELOG.md`'s matching entry for full detail. No implementation code exists; Modules 01–06 remain untouched and permanently frozen. Implementation requires its own separate, explicit future approval before it begins.
- **2026-07-11** — Pipeline 0.6.0. Module 06 (Confidence & Review) released at v1.0.0 — sixth module in the pipeline, frozen and feature-complete, production-ready for both interactive Claude-assisted and autonomous/unattended operation identically (Module 06 has no provider of any kind, the narrowest attack surface of any module built so far — see `Release/Module06/KNOWN_LIMITATIONS.md`). One post-freeze design correction (before any implementation code was written): `compute_score()`'s formula sign was inverted relative to `Rules/Confidence Rules.md`'s own worked example; corrected to `100 + sum(deductions.values())`. A reconstructed Independent Implementation Audit (the written record was found missing during the Release Audit and rebuilt from surviving evidence, not invented — see `Release/Module06/IMPLEMENTATION_AUDIT.md`) found and resolved 2 Medium design-committed test-coverage gaps; zero implementation defects found during Integration Testing (22/22 cases, two harness-authoring errors found and fixed instead). UAT's first run stopped on a genuine, real, Critical finding — in Module 01, not Module 06 (re-scanning an already-processed file silently discarded every downstream module's work) — resolved as Module 01's own separately-versioned post-freeze correction #1 (`v1.0.0` → `v1.0.1`, see the entry directly below), after which a full UAT restart completed with zero defects. A final independent Release Audit, performed across three restart cycles (each surfacing and resolving one genuine documentation/evidence finding: the missing Implementation Audit record; this ledger's own staleness; a missing performance measurement — 75 real files through the full Module 01→06 chain at 40.122 seconds, +1.0% versus Module 05's 39.711-second baseline), converged on a clean 13/13 Pipeline Contract Verification pass with zero Critical/High/Medium/Low findings remaining (see `Release/Module06/RELEASE_AUDIT.md`). Full regression suite 352/352 passing. Modules 07–08 not yet started.
- **2026-07-11** — Pipeline 0.5.0 (unchanged, superseded by the entry above). Module 06 (Confidence & Review) — no version change (not yet released, still `—`); status updated from "Not started" to "Release Audit in progress." Documentation-consistency correction: this ledger had not been updated in step with Module 06's real progress recorded elsewhere (`Governance/PROJECT_ROADMAP.md`, `CHANGELOG.md`, `src/README.md`), and the Module 01 patch entry immediately below still read "Module 06 UAT has not yet been restarted — awaiting separate explicit approval," which was superseded without this ledger being told. For the record, preserved here rather than by editing that entry: Module 06's UAT was subsequently restarted from Run 1 (Run 2), completed with zero findings, and was approved to proceed to Release Audit; Module 06's Release Audit is now in progress (see `Governance/PROJECT_ROADMAP.md`'s "Current module" section and `Build-out/06 Confidence & Review/Module 06 Implementation Audit.md` for full detail). This entry does not alter or retract the Module 01 patch entry below, which remains an accurate record of what was true when it was written.
- **2026-07-11** — Pipeline 0.5.0 (unchanged). Module 01 (Watch & Ingest) re-released at **v1.0.1** — a PATCH-level post-freeze correction under `Governance/FROZEN_MODULE_CHANGE_POLICY.md`, not a new module or a pipeline milestone. Classified Critical severity: `build_file_record()` re-scanning an already-tracked file silently discarded every downstream-owned field (Modules 02–06's work), discovered by Module 06 UAT's idempotency check (Finding UAT-1, UAT stopped and archived rather than continued past the finding). An Independent Root Cause Analysis, performed fresh rather than trusting the reporting UAT's own conclusion, confirmed the defect as primarily a Module 01 implementation gap and identified Option A2 ("mutate the existing record in place, touching only Module 01-owned fields") as the smallest correction. Fixed with two branches — content unchanged: in-place update of Module 01's own fields only, downstream fields never touched; content changed: same, plus an explicit reset of all 17 downstream-owned fields to their defaults, so the record re-enters Modules 02–06's existing null-based reprocessing path. `Release/Module01/MODULE_CONTRACT.md`'s guarantee shape is unaffected (still "never touches downstream fields," now precisely qualified for the re-scan case it was previously silent on) — PATCH, not MINOR/MAJOR, per this project's own Module 05 precedent for a shape-preserving contract clarification. Two new regression tests added (`test_build_file_record_preserves_downstream_fields_on_unchanged_rescan`, `test_build_file_record_clears_downstream_fields_on_content_change`), both confirmed load-bearing via mutation testing; full regression suite 352/352 passing. A targeted Independent Implementation Audit found the fix clean, disclosing one new Low-severity, pre-existing, out-of-scope observation (`content_hash` cleared during an "unreadable" episode can cause `content_changed` under-detection — see `Release/Module01/KNOWN_LIMITATIONS.md`). See `Release/Module01/RELEASE_NOTES.md`'s "Post-freeze correction #1" and `CHANGELOG.md`'s matching 2026-07-11 entry for full detail. Module 06 UAT has not yet been restarted — awaiting separate explicit approval.
- **2026-07-09** — Pipeline 0.5.0. Module 05 (Naming & Destination) released at v1.0.0 — fifth module in the pipeline, frozen and feature-complete, production-ready for both interactive Claude-assisted and autonomous/unattended operation identically (Module 05 has no provider of any kind, the same architectural departure Module 04 established for itself — see `Release/Module05/KNOWN_LIMITATIONS.md`). One genuine defect found and correctly disclosed rather than silently patched: UAT's first run stopped on a genuine, real finding (Finding UAT-1, whitespace stripped instead of converted to underscore in sanitized filenames), independently verified as a design-completeness gap and resolved via a disclosed post-freeze design-correction cycle (post-freeze correction #1), after which a full UAT restart completed with zero defects and additionally verified idempotency and a dedicated adversarial sanitization pass. A first Independent Implementation Audit found and resolved 3 Medium and 2 Low findings (fallback field-name accuracy, truncation correctness, missing committed test cases, a weak test assertion, duplicated override logic); zero implementation defects found during Integration Testing. A final independent release audit, performed in two passes, found and resolved 4 Medium and 1 Cosmetic documentation/evidence findings, then re-verified all 13 Pipeline Contract Verification checks passing (see `Release/Module05/IMPLEMENTATION_AUDIT.md`/`RELEASE_AUDIT.md`). Modules 06–08 not yet started.
- **2026-07-08** — Pipeline 0.4.0. Module 04 (Duplicate & Version Detection) released at v1.0.0 — fourth module in the pipeline, frozen and feature-complete, production-ready for both interactive Claude-assisted and autonomous/unattended operation identically (Module 04 has no provider of any kind — see `Release/Module04/KNOWN_LIMITATIONS.md`). Two genuine defects found and correctly disclosed rather than silently patched: a first Independent Implementation Audit found and resolved 1 High and 3 Medium findings (idempotency, an undisclosed candidate tie-break, non-exhaustive immutability tests, missing committed test cases); zero implementation defects found during Integration Testing (2 test-harness bugs found and fixed instead); UAT's first run stopped on a genuine, independently-reproduced defect (Finding UAT-1, near-duplicate detection not category-scoped), which was independently verified as a design-completeness gap and resolved via a disclosed post-freeze design-correction cycle (post-freeze correction #4), after which a full UAT restart completed with zero defects; a final independent release audit found and resolved 2 Medium documentation-completeness findings (see `Release/Module04/IMPLEMENTATION_AUDIT.md`/`RELEASE_AUDIT.md`). Modules 05–08 not yet started.
- **2026-07-06** — Pipeline 0.3.0. Module 03 (Metadata Extraction) released at v1.0.0 — third module in the pipeline, frozen and feature-complete, production-ready for interactive Claude-assisted operation (no autonomous provider exists in v1 — see `Release/Module03/KNOWN_LIMITATIONS.md`). Zero implementation defects found during integration testing (4 test-harness bugs found and fixed instead); zero defects found during UAT; an independent implementation audit before integration testing found and resolved 2 Medium findings; an independent release audit before freeze found and resolved 3 Medium and 2 Low findings (see `Release/Module03/IMPLEMENTATION_AUDIT.md`/`RELEASE_AUDIT.md`). Modules 04–08 not yet started.
- **2026-07-06** — Pipeline 0.2.0. Module 02 (Classification) released at v1.0.0 — second module in the pipeline, frozen and feature-complete, production-ready for interactive Claude-assisted operation (no autonomous provider exists in v1 — see `Release/Module02/KNOWN_LIMITATIONS.md`). One defect found and fixed during integration testing (unwrapped image-read failures in `ClassificationEngine`); zero defects found during UAT; an independent release audit before freeze found and resolved 3 High and 3 Medium findings (see `Release/Module02/RELEASE_AUDIT.md`/`RELEASE_AUDIT_2.md`). Modules 03–08 not yet started.
- **2026-07-06** — Pipeline 0.1.0. Module 01 (Watch & Ingest) released at v1.0.0 — first module in the pipeline, frozen and production-ready. Modules 02–08 not yet started.
