# DEPRECATED — renamed to pipeline/reporting.py to avoid shadowing Python's stdlib
# `logging` module (see CHANGELOG.md).
# Kept as a placeholder only because files can't be deleted from this workspace.
