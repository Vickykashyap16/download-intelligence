# Version History

**Pipeline Version: 0.8.0**

Fixture engine version inside the supported range used by
AppLifecycleControllerTests, so the lifecycle reaches the Load
Configuration phase's real decision rather than failing the version
gate first.
