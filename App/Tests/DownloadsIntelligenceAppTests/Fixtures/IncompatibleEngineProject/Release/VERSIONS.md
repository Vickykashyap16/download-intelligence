# Version History

**Pipeline Version: 9.9.9**

Fixture engine version deliberately outside the supported range used by
AppLifecycleControllerTests, so the lifecycle routes to the Error State
before any artifact beyond the version file is ever read.
