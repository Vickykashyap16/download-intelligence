# Version History

**Pipeline Version: 0.8.0**

Fixture engine version inside the supported range used by
FirstRunExperienceViewModelTests, so the flow reaches real `config`
subprocess invocations rather than failing the version gate first.
