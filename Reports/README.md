# ⚠️ Deprecated — superseded by `Runtime/Reports/`

This folder (and its subfolders) is no longer used. Kept in place only because files here can't be deleted from this workspace.

Human-readable reports now live under `Runtime/Reports/` (Daily Summary, Weekly Summary, Duplicate Report, Storage Report), as part of the `src/`-era restructure that grouped all operational output under `Runtime/`. See `Runtime/README.md`.

Ignore this folder going forward.
