# ⚠️ Deprecated — superseded by `Runtime/Logs/`

This folder (and its `batch_summaries/` subfolder) is no longer used. Kept in place only because files here can't be deleted from this workspace.

The action log now lives in `Runtime/Logs/action_log.jsonl`, as part of the `src/`-era restructure that grouped all operational output under `Runtime/`. See `Runtime/README.md`.

Ignore this folder going forward.
