# ⚠️ Deprecated — superseded by `Database/Learning/`

This folder is no longer used. Kept in place only because files here can't be deleted from this workspace.

User corrections now live in `Database/Learning/User Corrections.json`, as part of the `src/`-era restructure that grouped all persistent structured data under `Database/`. See `Database/Learning/README.md`.

Ignore this folder going forward.
