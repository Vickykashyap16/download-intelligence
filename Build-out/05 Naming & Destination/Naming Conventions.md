# ⚠️ Deprecated — superseded by `Rules/Naming Rules.md`

This folder is no longer used. It's kept in place only because files here can't be deleted from this workspace.

Naming templates now live in `Rules/Naming Rules.md`, alongside the other business rules (Classification, Folder, Confidence, Ignore). Destination folder taxonomy lives in `Rules/Folder Rules.md`.

Ignore this file going forward.
