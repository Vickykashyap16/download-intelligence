# Stray empty file created accidentally while troubleshooting a test run — not needed
# (Python 3 namespace packages mean src/ doesn't need one either). Harmless and inert
# since nothing imports Build-out/ as a package; kept only because files can't be
# deleted from this workspace.
